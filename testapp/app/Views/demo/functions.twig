<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Functions Demo</title>
    <style>
        body { font-family: system-ui, sans-serif; max-width: 800px; margin: 2rem auto; padding: 0 1rem; }
        h1, h2 { color: #333; }
        .example { background: #f5f5f5; padding: 1rem; border-radius: 4px; margin: 1rem 0; font-family: monospace; }
        .result { background: #e3f2fd; padding: 0.5rem; margin-top: 0.5rem; border-radius: 4px; }
        .nav { margin: 2rem 0; }
        .nav a { margin-right: 1rem; color: #1976d2; }
    </style>
</head>
<body>
    <h1>Functions & Filters Demo</h1>

    <h2>Custom Function: greet()</h2>
    <div class="example">
        <code>{{ '{{ greet("World") }}' }}</code>
        <div class="result">{{ greet("World") }}</div>
    </div>

    <h2>Custom Filter: reverse</h2>
    <div class="example">
        <code>{{ '{{ "Hello"|reverse }}' }}</code>
        <div class="result">{{ "Hello"|reverse }}</div>
    </div>

    <h2>Built-in CI4 Functions</h2>

    <div class="example">
        <code>{{ '{{ base_url("assets/style.css") }}' }}</code>
        <div class="result">{{ base_url("assets/style.css") }}</div>
    </div>

    <div class="example">
        <code>{{ '{{ site_url("users/profile") }}' }}</code>
        <div class="result">{{ site_url("users/profile") }}</div>
    </div>

    {% if debug is defined and debug %}
    <h2>Debug Mode</h2>
    <div class="example">
        <code>{{ '{{ dump(items) }}' }}</code>
        <div class="result">Debug is enabled - use dump() to inspect variables</div>
    </div>
    {% endif %}

    <div class="nav">
        <a href="{{ base_url('twigdemo/variables') }}">← Variables Demo</a>
        <a href="{{ base_url('twigdemo') }}">Back to Index</a>
    </div>
</body>
</html>
