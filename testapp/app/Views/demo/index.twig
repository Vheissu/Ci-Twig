<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    <style>
        body { font-family: system-ui, sans-serif; max-width: 800px; margin: 2rem auto; padding: 0 1rem; }
        h1 { color: #333; }
        .message { background: #e8f5e9; padding: 1rem; border-radius: 4px; margin: 1rem 0; }
        ul { background: #f5f5f5; padding: 1rem 2rem; border-radius: 4px; }
        li { margin: 0.5rem 0; }
        .nav { margin: 2rem 0; }
        .nav a { margin-right: 1rem; color: #1976d2; }
    </style>
</head>
<body>
    <h1>{{ title }}</h1>

    <div class="message">
        {{ message }}
    </div>

    <h2>Requirements</h2>
    <ul>
        {% for item in items %}
            <li>{{ item }}</li>
        {% endfor %}
    </ul>

    <div class="nav">
        <h3>More Demos</h3>
        <a href="{{ base_url('twigdemo/variables') }}">Variables Demo</a>
        <a href="{{ base_url('twigdemo/functions') }}">Functions Demo</a>
    </div>
</body>
</html>
